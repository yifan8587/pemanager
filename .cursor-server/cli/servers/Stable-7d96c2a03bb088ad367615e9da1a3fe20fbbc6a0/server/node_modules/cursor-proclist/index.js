// Copyright Anysphere Inc.

/**
 * JavaScript wrapper for the native cursor_proclist module
 *
 * Returns an array of process tuples, each with format:
 * [pid, ppid, name, extensionId, cpuTimeMs, memoryMB]
 */

const binding = require('./build/Release/cursor_proclist.node');

module.exports = {
	cursor_proclist: binding.cursor_proclist
};

